import React from "react";
import Router from "../Routes";

const RootContainer = props => <Router {...props} />;

/**
 * This is where you add the root consumers (like react-redux root connect())
 */
export default RootContainer;
